# @connectingmatrix/file final runtime contracts

This file is generated by the final process-monitor / project / nodes / workflow-AI / Giga-agents hardening pass.

## Ownership

- file upload provider
- Supabase/drive provider adapters
- MIME detection
- format processors
- ZIP/source archive create/extract
- node package encode/decode
- project source archive exclusion policy

## Public API contracts

- `Files.upload(input,options)`
- `Files.process(file)`
- `Files.detectMime(fileName,bytes?)`
- `Files.registerProcessor(mimeOrExt,processor)`
- `Files.createSourceArchive(files,options)`
- `Files.extractSourceArchive(archive)`
- `Files.shouldArchivePath(path)`
- `Files.createNodePackage(node)`
- `Files.extractNodePackage(packageData)`
- `createSupabaseStorageProvider(config)`
- `createMemoryStorageProvider()`

## Routes / launcher

- `GET /file/health`
- `POST /file/upload`
- `POST /file/process`
- `GET /file/launcher`

## Notes

- Runtime ZIP/MIME/archive code should live here only; projects, drive, ai-agents, and nodes delegate to file.

## Stub launcher

Run the local package launcher with:

```bash
npm run build
node playground.mjs
```
